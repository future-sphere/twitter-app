import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
  contentContainer: {
    paddingVertical: 10,
    paddingHorizontal: 8,
    marginVertical: 5,
    marginHorizontal: 10,
    backgroundColor: '#fff',
    borderRadius: 8,
  },
});

export default globalStyles;
